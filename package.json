{
  "name": "pi-streak",
  "version": "1.2.2",
  "type": "module",
  "description": "pi-streak leaderboard API + CLI",
  "main": "src/index.ts",
  "bin": {
    "pi-streak": "src/cli/index.ts"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cli": "bun src/cli/index.ts",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.12.23"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  },
  "files": [
    "src",
    "wrangler.jsonc",
    "schema.sql"
  ],
  "license": "MIT",
  "engines": {
    "bun": ">=1.0.0"
  }
}